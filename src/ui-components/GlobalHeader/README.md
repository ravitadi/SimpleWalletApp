The component loads header for the Page. The reason this was separated is to modularize the component and can be reuse else where in the applicaiton.

## Dependencies
  - Material UI

### Usage:

```<GlobalHeader /> ```
